# collectZ v3.16.26

## Version and date

- Version: `3.16.26`
- Date: 2026-06-07

## Milestone target and status

- Roadmap slice: `3.16.26 — CodeQL Media Title Sanitizer Precision`
- Status: completed

## Summary

This patch continues CodeQL baseline remediation by replacing media lookup-title descriptor unwrapping with bounded scanner logic that preserves intended title candidate behavior.

## What changed

- Replaced regex-based parenthetical and bracket descriptor unwrapping in media lookup title normalization with a bounded scanner.
- Preserved short descriptor text for TMDB title candidate matching while keeping an explicit descriptor length limit.
- Added backend unit source coverage for the scanner-based lookup-title contract.

## Breaking changes

- None.

## Environment/config changes

- No environment changes.

## Migration and data impact

- No schema, migration, or data impact.

## Deployment and verification

- Rebuild backend and frontend containers with `APP_VERSION=3.16.26`.
- Verify `/api/health` reports version/frontend/backend/build `3.16.26`.
- Verify Help > Releases serves `3.16.26`.
- Confirm local CodeQL reports no `js/incomplete-multi-character-sanitization` findings.

## Rollback guidance

- Revert to `3.16.25` if imported/exported media title lookup candidates regress.
- No schema rollback is required.

## Security vulnerability triage

- Clears the local CodeQL media title `js/incomplete-multi-character-sanitization` findings by removing the regex pattern that resembled incomplete multi-character sanitization.
- This normalization helper is used for provider lookup-title candidates, not direct HTML rendering.

## Known issues and follow-up

- Broader CodeQL baseline remediation remains open for request-forgery disposition/modeling, unused-variable intent triage, and lower-priority quality findings.

## Verification

- Backend syntax check.
- Backend unit tests.
- Local CodeQL CLI JavaScript/TypeScript security plus security-and-quality analysis.
- Docker backend unit tests.
- Docker OpenAPI validation.
- Help > Releases smoke.
- Version sync and release feed regeneration.
- `git diff --check`.
