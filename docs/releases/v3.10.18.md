# collectZ v3.10.18

## Version and date

- Version: `3.10.18`
- Date: 2026-06-02
- Milestone: `3.10.18 — Browser Regression Navigation Stabilization`

## Milestone target and status

- Status: `completed`

## Summary

This patch stabilizes the admin shell browser regression after the recent desktop/mobile header work by making dashboard tab navigation in the test follow the app's mounted route-sync behavior.

## What changed

- Added a small Playwright helper that dispatches dashboard route state after query-tab navigation.
- Updated the mobile compact toolbar regression to use that helper.
- Updated the desktop library heading regression to use that helper.
- Updated the mobile utility header regression to use that helper.
- Corrected Capture Inbox expectations now that its page heading is intentionally visible.

## Breaking changes

None.

## Environment/config changes

None.

## Migration and data impact

No schema migration or data backfill is required. This patch changes browser regression coverage, version metadata, and release feed data only.

## Deployment and verification

- Verify `/api/health` reports frontend/backend/build `3.10.18` from the running stack.
- Verify Help > Releases serves `3.10.18`.
- Verify the targeted admin shell browser regression cases pass.

## Rollback guidance

Rollback to `3.10.17` is safe because this patch has no schema, runtime, or data-format changes.

## Known issues and follow-up

- The earlier RBAC CI failure was a Docker Hub pull timeout before the RBAC app check started; rerun CI or add registry pull retry/mirror behavior if it repeats.

## Security vulnerability triage

- High findings: none identified in local checks for this slice so far.
- Owner: collectZ maintainers.
- Target remediation milestone: before `3.10.18` closeout if any high findings appear.
- No auth, scope, token, provider-write, notification, or outbound-link behavior changed.
- No secrets or provider credentials are added to release artifacts.

## Verification

- Targeted admin shell browser regression passed for mobile compact library toolbar, desktop section headings, and mobile utility stable headers.
- Docker rebuild at `APP_VERSION=3.10.18` completed for backend and frontend.
- Running `/api/health` reported frontend/backend/build `3.10.18`.
- Help > Releases smoke found `3.10.18` as the latest served release.
- Backend unit tests passed.
- OpenAPI validation passed.
- RBAC regression passed from the backend container against `BASE_URL=http://frontend:3000`.
- Platform edition boundary smoke passed from the running platform stack.
