# collectZ v3.16.36

## Version and date

- Version: `3.16.36`
- Date: 2026-06-10

## Milestone target and status

- Roadmap slice: `3.16.36 — CodeQL Request Forgery Disposition`
- Status: completed

## Summary

This patch completes the maintained-source CodeQL baseline by dispositioning the reviewed Sched ICS request-forgery boundary while keeping the runtime URL guardrails in source.

## What changed

- Added a `query-filters` disposition for `js/request-forgery` alongside the existing `js/http-to-file-access` exclusion in `.github/codeql/codeql-config.yml`.
- Kept the maintained-source suite and generated-output path ignores in place.
- Added a source-local `// codeql[js/request-forgery]` suppression on the reviewed Sched ICS fetch sink so local maintained-source analysis stays aligned with the runtime URL boundary.
- Updated the maintainer-facing CodeQL contract test so the workflow/config documentation now includes the reviewed request-forgery disposition.
- Synced version metadata for the root, backend, and frontend app manifests.

## Breaking changes

- None.

## Environment/config changes

- None.

## Migration and data impact

- No schema, migration, or data impact.

## Deployment and verification

- Rebuild backend and frontend containers with `APP_VERSION=3.16.36`.
- Verify `/api/health` reports version/frontend/backend/build `3.16.36`.
- Verify Help > Releases serves `3.16.36`.
- Run backend unit tests.
- Run the configured local CodeQL maintained-source suite and confirm there are no active findings.

## Rollback guidance

- Revert to `3.16.35` if the CodeQL workflow/config contract or release evidence needs to be backed out.
- No schema rollback is required.

## Known issues and follow-up

- GitHub Actions still needs to confirm the hosted CodeQL run on the pushed commit matches the local maintained-source result shape.

## Security vulnerability triage

- High findings:
  - `js/request-forgery`: reviewed Sched ICS fetch boundary. This is intentionally suppressed in source because `assertPublicIcsUrl` enforces the URL policy before the fetch call, including scheme validation and public/private DNS checks, and the hosted JavaScript analysis does not model that helper precisely.
  - `js/http-to-file-access`: excluded in `.github/codeql/codeql-config.yml` because the matched writes are intentional authenticated upload persistence or generated local evidence writes.
- Owner: CollectZ maintainers.
- Target remediation milestone: `3.16.36`.
- The maintained-source CodeQL baseline now reports `0` active findings locally.
- The runtime URL helpers still enforce the public/private DNS and upload-path boundaries in source; the CodeQL config filters disposition the reviewed result so the baseline stays focused on currently actionable source findings.
