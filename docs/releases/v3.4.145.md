# collectZ v3.4.145

- Version: `3.4.145`
- Status: `released`

## Summary

This patch adds a read-only Plex provider/API import parity proof before any future migration away from the current library-section import path.

## Changes

- Adds a fake-PMS parity smoke comparing `/media/providers` discovery to the current Plex legacy import readbacks.
- Documents that provider discovery can expose PMS capabilities but does not enumerate importable item rows by itself.
- Confirms the current legacy import paths still expose duplicate-safe identities, artwork keys, media variants, and TV season/episode coverage.
- Keeps Plex import behavior, reconciliation sync, watched-state writeback, rating writeback, and automatic sync behavior unchanged.
- Writes secret-safe parity evidence without Plex tokens, provider URLs, private IPs, or media file paths.

## Security vulnerability triage

- High findings: none identified locally; dependency preflight remains the local source for dependency audit status.
- Owner: collectZ maintainers.
- Target remediation milestone: none required for this release.
- This change is read-only and does not expose Plex tokens, raw Plex URLs, media file paths, or raw PMS source payloads in browser-visible responses.

## Verification Notes

- Verify `/api/health` reports frontend/backend/build `3.4.145`.
- Verify Help > Releases serves `v3.4.145`.
- Verify the Plex provider import parity smoke reports `readOnly=true`, `importMutation=false`, and `plexWriteback=false`.
- Verify the parity decision keeps legacy import current until provider/API item listing reaches field parity.
