# collectZ v3.20.9

## Summary

Moved Plex conflict review match-reason filtering into the backend query.

## Changes

- Added `matchedBy` filtering to the Plex reconciliation conflict-review endpoint.
- Returned match-reason filter counts for the selected open/resolved status.
- Updated the Plex Sync conflict review dropdown to show backend counts.
- Kept attach, create separate, and dismiss behavior unchanged.

## Verification

- Backend media route and unit-test source syntax passed.
- Frontend production build passed.
- Release feed regenerated.
- Backend unit/source suite passed with `333` checks.
- Local 3201 stack rebuilt to `3.20.9`; `/api/health` reports frontend/backend/build `3.20.9`; backend logs report schema `114` is up to date.

## Notes

- Filtering now applies before the endpoint limit, so the UI no longer filters only the first loaded page.
- Skipped/provider-error rows that are not persisted as conflict-review rows remain visible through sync job summaries.
