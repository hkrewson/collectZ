# v3.4.25

## Version and date
- Version: `3.4.25`
- Date: `2026-04-27`

## Milestone target and status
- Target milestone: `3.4.25 — Release Evidence Token Hygiene Cleanup`
- Included shipped scope:
  - `3.4.25 — Release Evidence Token Hygiene Cleanup`
- Status: `Completed`

## Summary
This release cleans up old release evidence examples so fixed local Playwright bypass token values are no longer preserved in committed roadmap or release-note command examples.

## What changed
### Release evidence token hygiene
- Redacted older fixed `PLAYWRIGHT_E2E_BYPASS_TOKEN` examples in roadmap closeouts and release notes.
- Kept command shapes visible for maintainers while replacing token material with `<redacted>`.
- Added a backend unit source guard that fails if fixed local Playwright bypass token examples return to roadmap or release-note docs.
- Added a short UI/UX cleanup working plan to the backlog so the next drawer/mobile/image tasks can be promoted in a deliberate order.

## Breaking changes
- None intended. Runtime behavior and API contracts are unchanged.

## Environment/config changes
- None. CI and local Playwright bypass behavior still rely on caller-provided or generated token values.

## Migration and data impact
- No database migration is introduced by this release.
- No data migration or volume change is required.

## Deployment and verification
1. Rebuild/redeploy through the Docker compose workflow.
2. Verify `/api/health` reports `3.4.25`.
3. Verify Help > Releases includes `3.4.25`.
4. Run backend unit tests and confirm the release-doc token hygiene guard passes.

## Rollback guidance
- Roll back to the previous image tag if release metadata display regresses.
- No data rollback is required.

## Security vulnerability triage
Production/runtime release gates for `3.4.25`:
- backend production dependency audit: local preflight and CI dependency-scan required for authoritative release verdict
- frontend production dependency audit: local preflight and CI dependency-scan required for authoritative release verdict

High findings: None newly introduced by the intended `3.4.25` shipped scope.
Tracked moderate findings:
- follow dependency-scan and image-security gates in CI for the authoritative release verdict

Owner: maintainers
Target remediation milestone: follow the next dependency/security follow-up if CI surfaces new findings

## Known issues and follow-up
- This release only redacts the fixed token examples already found in committed docs and adds a guard for roadmap/release notes. Broader generated-artifact secret hygiene remains covered by existing release/checklist policy.
