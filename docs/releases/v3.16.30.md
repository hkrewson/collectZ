# collectZ v3.16.30

## Version and date

- Version: `3.16.30`
- Date: 2026-06-07

## Milestone target and status

- Roadmap slice: `3.16.30 — CodeQL Maintained-Source Quality Finding Triage`
- Status: completed

## Summary

This patch clears the maintained-source CodeQL quality findings for trivial conditionals, template-looking strings, incompatible comparisons, duplicate object properties, and useless comparisons without changing intended runtime behavior.

## What changed

- Removed unreachable fallback checks where surrounding branches already prove active invite, media, library, support-thread, and log-export values.
- Fixed the Art filter strip and shared `Icons.Check` definition so the rendered behavior stays the same without duplicate guard/object-property noise.
- Rewrote the Playwright support helper's detached-library fixture script to build suffix-based names explicitly instead of embedding template-looking text inside ordinary strings.
- Updated source-shape unit coverage for the clarified default-scope library comparison.

## Breaking changes

- None.

## Environment/config changes

- None.

## Migration and data impact

- No schema, migration, or data impact.

## Deployment and verification

- Rebuild backend and frontend containers with `APP_VERSION=3.16.30`.
- Verify `/api/health` reports version/frontend/backend/build `3.16.30`.
- Verify Help > Releases serves `3.16.30`.
- Run backend unit tests and the frontend production build.
- Run the configured local CodeQL maintained-source analysis and confirm the targeted quality classes report `0` findings.

## Rollback guidance

- Revert to `3.16.29` if support-thread triage/reply rendering, Art filter display, default-scope library selection, or detached Playwright support fixtures regress.
- No schema rollback is required.

## Security vulnerability triage

- Authoritative CodeQL baseline remains the GitHub Actions clean checkout of committed maintained app source.
- Local maintained-source CodeQL now reports `22` total findings, with `0` targeted maintained-source quality findings for `js/trivial-conditional`, `js/template-syntax-in-string-literal`, `js/comparison-between-incompatible-types`, `js/overwritten-property`, and `js/useless-comparison-test`.
- Remaining CodeQL findings are outside this quality cleanup slice and continue through the broader baseline-remediation backlog.
