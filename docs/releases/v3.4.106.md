# collectZ v3.4.106

- Version: `v3.4.106`
- Date: `2026-05-06`
- Status: `Ready for CI release gates`

## Kavita Reset Progress Runtime Proof

## Summary

This patch proves the Kavita reset-progress payload shape in the existing fake Kavita-compatible runtime probe without exposing a user-facing reset or unread action.

## What Changed

### Shipped

- Extended the Kavita progress/read-state contract with a probe-only reset-progress payload.
- Added fake-runtime proof for `POST /api/Reader/progress` with `pageNum: 0` and `bookScrollId: null`.
- Confirmed the reset-progress probe does not call bulk unread endpoints.
- Kept chapter unread and reset-progress UI/routes disabled until real Kavita runtime semantics are proven.

## Security vulnerability triage

Backend dependency audit: 2 moderate production advisories, 0 high, 0 critical.

Frontend dependency audit: 0 production advisories.

High findings: 0.

Critical findings: 0.

Owner: maintainers

Target remediation milestone: no new remediation milestone for this proof-only slice; the existing backend moderate advisories remain below the release-blocking high/critical threshold.

## Boundaries

- No `kavita-reset-progress` runtime route was added.
- No `Reset Kavita progress` or `Mark Unread in Kavita` UI control was added.
- No series, volume, multiple-volume, multiple-series, panel, or KOReader unread endpoint was enabled.
- No claim is made that page `0` is true unread in real Kavita.

## Verification Notes

- Docker-first release closeout ran the Kavita progress/read-state contract probe, backend source assertions, OpenAPI validation, Help > Releases smoke, dependency audits, and the relevant release regression gates for this proof-only implementation slice.
