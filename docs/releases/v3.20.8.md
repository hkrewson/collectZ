# collectZ v3.20.8

## Summary

Persisted Plex readback refresh cadence in the Plex Sync settings.

## Changes

- Added stored Plex readback refresh settings for enablement, interval, and max items.
- Updated the Plex watched-state/rating readback scheduler to use stored settings unless env overrides are present.
- Added compact Plex Sync controls for scheduled readback refresh cadence.
- Kept manual readback refresh independent from scheduled enablement.

## Verification

- Backend route/service/migration/unit-test source syntax passed.
- Frontend production build passed.
- Release feed regenerated.
- Backend unit/source suite passed with `333` checks.
- Local 3201 stack rebuilt to `3.20.8`; `/api/health` reports frontend/backend/build `3.20.8`; backend logs show migration `v114` applied.

## Notes

- Existing installs default scheduled readback refresh to off.
- Changing the saved interval affects effective config readback immediately, but an already-running scheduler timer uses its startup interval until the backend restarts.
