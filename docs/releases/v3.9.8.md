# collectZ v3.9.8

## Version and date

- Version: `3.9.8`
- Date: 2026-05-21
- Milestone: `3.9.8 — Observability Collector Smoke Wiring`

## Milestone target and status

Status: Complete.

This patch fixes the release evidence path for structured-log collector smoke tests by making the generated CI compose override pass log-export runtime settings into the backend container.

## Changes

- Forwarded `LOG_EXPORT_BACKEND`, host, port, debug, and read-only settings through the generated CI Docker Compose backend override.
- Added unit coverage so the generated CI compose writer and resolved local CI override keep the external-log env wiring visible.
- Regenerated observability release evidence with Graylog, Loki, syslog, nonblocking export failure, and restore checks passing.

## Security and privacy

- No production default log-export behavior changes; public homelab compose still keeps external log export off unless configured.
- Smoke credentials remain ephemeral and redacted in generated evidence.
- The fix is limited to CI/release evidence runtime wiring.

## Security vulnerability triage

- High findings: none identified in local checks for this slice.
- Owner: collectZ maintainers.
- Target remediation milestone: before `3.9.8` closeout if any high findings appear.

## Verification

- Verify generated CI compose forwards log-export env values into backend runtime.
- Verify observability release evidence passes all collector smoke checks.
- Verify `/api/health` reports frontend/backend/build `3.9.8` from the running stack.
- Verify Help > Releases serves `3.9.8`.

## Rollback

- Restore `3.9.7` version metadata, release feed, generated CI compose writer, unit assertions, and release notes.

## Follow-ups

- CI remains authoritative for hosted `compose-smoke`, `secret-scan`, and image security/SBOM gates.
