# v2.0.0-alpha.9

- Date: 2026-02-25
- Version: `2.0.0-alpha.9`
- Scope: failure telemetry and tester issue-triage support

## Added

- Explicit audit events for validation failures:
  - `request.validation.failed`
- Explicit audit events for auth/access denials:
  - `auth.access.denied` (`missing_token`, `invalid_or_expired_session`)
  - `auth.permission.denied` (`insufficient_permissions`)
- Tester issue template document:
  - `docs/wiki/18-Tester-Bug-Template.md`

## Changed

- Smoke test checklist now references the tester bug template for consistent defect reporting.
- Version metadata bumped to `2.0.0-alpha.9` across app metadata, package versions, and compose defaults.

## Verification

- `node --check backend/middleware/auth.js`
- `node --check backend/middleware/validate.js`
- `docker compose --env-file .env up -d --build`
- Triggered failure-path checks confirm Activity log entries for:
  - validation failures
  - denied/unauthorized requests
