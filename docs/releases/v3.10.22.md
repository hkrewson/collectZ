# collectZ v3.10.22

## Version and date

- Version: `3.10.22`
- Date: 2026-06-03
- Milestone: `3.10.22 — Dependency PR Cleanup`

## Milestone target and status

- Status: `completed`

## Summary

This patch consolidates the safe dependency and workflow updates from the open PR backlog so the remaining pull requests are easier to reason about.

## What changed

- Updated backend runtime dependencies for Express 4, AWS S3, Nodemailer, Helmet, Postgres, Axios, Fast XML Parser, rate limiting, and CSV parsing.
- Updated frontend Vite to the current open patch release.
- Updated CI workflow actions for upload-artifact and Trivy.
- Left major or compatibility-sensitive PRs out of this slice for separate review.

## Breaking changes

None.

## Environment/config changes

None.

## Migration and data impact

No schema migration or data backfill is required. This patch changes dependency and CI workflow inputs only.

## Deployment and verification

- Verify backend and frontend dependency installs complete from lockfiles.
- Verify frontend build still completes.
- Verify backend unit/OpenAPI coverage still passes.
- Verify `/api/health` reports frontend/backend/build `3.10.22` from the running stack.
- Verify Help > Releases serves `3.10.22`.

## Rollback guidance

Rollback to `3.10.21` is safe because this patch has no schema or data-format changes.

## Known issues and follow-up

- React 19, Tailwind 4, Express 5, Zod 4, and ZXing package updates remain separate review items.
- The ZXing PR pair currently has a peer dependency conflict and should not be merged together as-is.

## Security vulnerability triage

- High findings: none identified in local dependency audit for this slice.
- Owner: collectZ maintainers.
- Target remediation milestone: before `3.10.22` closeout if any high findings appear.
- The Snyk Express 4 security PR is addressed by updating Express to `4.22.2`.
- No auth, scope, token, provider-write, notification, or outbound-link behavior changed.
- No secrets or provider credentials are added to release artifacts.

## Verification

- Backend `npm ci --no-fund` passed under Node 20 Docker.
- Frontend `npm ci --no-fund` passed under Node 20 Docker.
- Backend `npm audit --audit-level=high` passed with no vulnerabilities.
- Frontend `npm audit --audit-level=high` passed with no vulnerabilities.
- Backend unit tests passed.
- OpenAPI validation passed.
- Frontend Vite build passed under Node 20 Docker.
- Docker rebuild at `APP_VERSION=3.10.22` passed for backend and frontend.
- Running `/api/health` reports frontend/backend/build `3.10.22`.
- Help > Releases smoke passed with `3.10.22` as the latest release.
- RBAC regression passed when run alone against the running stack.
- Platform edition boundary smoke passed against the running platform stack.
- Homelab edition boundary was not rerun in this platform-configured stack; it requires an alternate edition runtime.
