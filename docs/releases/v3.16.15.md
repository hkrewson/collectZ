# collectZ v3.16.15

## Version and date

- Version: `3.16.15`
- Date: 2026-06-07

## Milestone target and status

- Roadmap slice: `3.16.15 — Library Navigation Utility Group`
- Status: completed

## Summary

This patch cleans up the Workspace sidebar by keeping primary library categories together and moving Loans, Review, and Wishlist into a separated Library utility group.

## What changed

- Moved `Loans` below the primary library category list.
- Renamed the visible `Merge Review` sidebar entry to `Review`.
- Moved `Review` into the Library section instead of showing it as a separate Workspace item.
- Kept the existing Merge Review route and page behavior.
- Kept `Wishlist` in the separated Library utility group.
- Kept `Import`, `Help`, and `Settings` as direct Workspace navigation items.
- Updated browser and unit coverage for the new navigation structure.

## Breaking changes

- None.

## Environment/config

- No required environment changes.

## Migration/data impact

- No schema migration.
- No data migration.

## Deployment/verification

- Release feed regenerated with `3.16.15`.
- Version metadata synchronized across backend, frontend, and app metadata.
- Docker runtime verification should confirm the running stack serves `3.16.15`.
- Help > Releases smoke should verify `3.16.15` is visible from the running stack.

## Security vulnerability triage

- High findings: none expected from this navigation-only patch.
- Owner: collectZ maintainers.
- Target remediation milestone: before `3.16.15` closeout if any high findings appear.
- This patch does not add secrets, credentials, auth flows, provider configuration, exports, imports, or new data exposure.

## Verification

- Sidebar browser coverage verifies `Review` replaces the standalone `Merge Review` nav entry while still opening the Merge Review surface.
- Unit coverage verifies the Library-owned Review navigation contract.
- Existing route behavior remains unchanged.

## Follow-ups

- None for this slice.
